mongoose = require 'mongoose'

schema = new mongoose.Schema
    sendDate:
        type: Date
        default: Date.now
    readDate:
        type: Date
        default: null
    from:
        type: String
        required: true
    message:
        type: String
        required: true

module.exports = mongoose.model('Message', schema)
